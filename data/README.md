# data/

This directory is a runtime working directory. At run time the system writes
cache files here (macro data caches, COT snapshots, and OHLCV dumps) produced
by MacroAgent and the data cache layer.

These files are generated locally and are not tracked in version control. The
directory is kept in the repository as an empty placeholder so the expected
path exists on first run.
