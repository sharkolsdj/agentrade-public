# scripts/

This directory holds operational scripts (startup, deployment, and
maintenance) used to run the system in production.

These scripts are environment specific and are intentionally not included in
this public repository.
